---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: winblues7
description: Winblues 7

base-image: ghcr.io/ublue-os/bazzite-dx
image-version: latest

modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        enable:
          - bazzite-org/bazzite 
          - bominic27644/aerothemeplasma-rpm
    install:
      packages:
      - kvantum
      - plymouth-plugin-script
      - aerothemeplasma-sounds
      - aerothemeplasma-icons
      - aerothemeplasma-desktop
      - aeroshell-workspace
      - aeroshell-smod
      - aeroshell-kwin-components
    replace:
      - from-repo: copr:copr.fedorainfracloud.org:bominic27644:aerothemeplasma-rpm
        packages:
          - libplasma

  - type: script
    snippets:
      - 'cd $CONFIG_DIRECTORY/scripts && for script in *.sh; do [ -f "$script" ] && bash "$script" || exit $?; done'

  - type: files
    files:
      - source: system
        destination: /

  # Regenerate initramfs because of plymouth
  - type: initramfs

  - type: signing
